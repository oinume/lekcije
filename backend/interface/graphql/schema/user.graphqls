type User {
  id: ID!
  email: String!
  followingTeachers(
    first:Int,
    after:String,
    last:Int,
    before:String
  ): FollowingTeacherConnection!
  notificationTimeSpans: [NotificationTimeSpan!]!
  showTutorial: Boolean!
}
